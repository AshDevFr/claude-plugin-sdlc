# Ruff configuration only; tools/specs is vendored as plain files, not packaged.
[tool.ruff]
line-length = 110
target-version = "py310"
# docs/ holds prose whose code blocks are illustrations, not code to format.
extend-exclude = ["docs"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
