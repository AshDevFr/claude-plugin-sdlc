#!/usr/bin/env python3
"""Entry point: tools/specs/specs <subcommand>. Needs nothing installed but PyYAML."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from sdlc_specs.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
