# Settings for the sdlc plugin, written by /sdlc:init.
tracker:
  # gitlab | github | linear: sets ticket key forms and ticket spec directory names
  system: $tracker
$tracker_options
code_host:
  # gitlab | github: sets where CODEOWNERS lives and the PR conventions
  system: $host
$host_options
$top_options
