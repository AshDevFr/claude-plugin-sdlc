# Scenario: open-questions

On branch `2026-09-23-webhook-retries`, a spec created from the intent has two open questions.
Use it to try `/sdlc:clarify`.

- `specs lint`: nothing to report (exit 0); with `--ready`, `L008` for each open question.
- `specs intent check`: intent `unchanged`.
