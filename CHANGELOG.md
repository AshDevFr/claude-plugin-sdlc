# Changelog

## Unreleased

- Plugin manifest and one-plugin marketplace.
- The `specs` helper: spec parsing, intent and ticket specs, lint rules L001 to L016,
  `new`, `intent check/record/assess`, `coverage`, `status`.
